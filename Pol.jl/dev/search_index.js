var documenterSearchIndex = {"docs":
[{"category":"section","location":"verbs/#Verbs","page":"Verbs","text":"A mutating verb takes its outputs first, as a NamedTuple it destructures — f!((; Y), X; kws...) — and describes them with outputs. Its Allocating form is the same verb with the first argument answering where results live instead of which arrays: a Space the outputs materialize from, before any frame opens.","title":"Verbs"},{"category":"type","location":"verbs/#Pol.Allocating","page":"Verbs","text":"Allocating(f!)\n\nThe allocating form of a mutating verb: a callable that owns the outputs/scratch ritual so call sites don't repeat it.\n\nconst decode_attention = Allocating(decode_attention!)\no = decode_attention(space, q, K, V; lengths)\n\nThe first argument answers where results live — the mutating verb takes its output buffers there, the allocating form takes a Space to materialize them from. Calling it allocates outputs from space — before any frame opens, so on an Arena they sit below the frame's mark and survive its retract — then runs f! and returns the outputs NamedTuple. A verb marked @takes_space runs inside a scratchspace block and receives the frame as its space keyword: its defaulted scratch is carved there and reclaimed at return.\n\nspace may itself be a Frame — frames are spaces and nest — in which case the outputs take the caller's frame lifetime.\n\nThe output_override keyword merges caller-owned arrays over the outputs spec before materialization, so an overridden entry is never allocated — pass the verb's own state array to run partially in place: f(space, args..., S; output_override = (; S′ = S)).\n\n\n\n\n\n","title":"Pol.Allocating"},{"category":"function","location":"verbs/#Pol.takes_space","page":"Verbs","text":"takes_space(f!) -> Bool\n\nWhether the mutating verb f! accepts a space keyword — the space its defaulted scratch materializes from. false generically; declare with @takes_space. Allocating branches on it: a verb that takes no space is called bare, with no frame opened on its behalf.\n\n\n\n\n\n","title":"Pol.takes_space"},{"category":"macro","location":"verbs/#Pol.@takes_space","page":"Verbs","text":"Pol.@takes_space f!\n\nDeclare that the mutating verb f! accepts a space keyword (see takes_space). One line next to the verb's scratch method:\n\nPol.@takes_space decode_attention!\n\n\n\n\n\n","title":"Pol.@takes_space"},{"category":"section","location":"scratchspaces/#Scratchspaces","page":"Scratchspaces","text":"This is the mechanism behind the lifetime split in the quick start: buffers a pass needs only while it runs are allocated inside the frame and die with the block, while buffers the caller must outlive are allocated from the unframed space before the block opens.\n\nBoth forms bind their frame as the ambient space for the block, so the ambient space is always the innermost open frame.","title":"Scratchspaces"},{"category":"function","location":"scratchspaces/#Pol.scratchspace","page":"Scratchspaces","text":"scratchspace(f, space)\n\nAn allocation frame on space: calls f with a Frame, which is also the ambient space for the block —\n\nscratchspace(space) do frame\n    softmax!(y, x; scratch(frame, softmax!, x)...)\nend\n\nOn an Arena the frame carries a mark, retracted when the block closes, also on a throw — everything carved inside is reclaimed. On any other space the frame records each buffer it materializes and release!s them all at close — a no-op for GC-owned arrays, eager reclamation where an overload provides it. Frames nest: opening a frame on a frame stacks a new one on the same space.\n\nInside the block the ambient space (ambientspace) is the frame: once a frame is open, nothing should allocate from outside it implicitly, so every frame-opening rebinds. The frame argument is therefore redundant for lifetime — but it is the concretely-typed spelling: ambient reads are dynamically dispatched, alloc(frame, ...) infers.\n\nBuffers allocated from the frame are only valid inside the block. Allocating from the unframed space — outputs before the block opens — is what gives a buffer the caller's lifetime.\n\n\n\n\n\nscratchspace(f)\n\nThe ambient form: a frame on ambientspace, rebound as ambient for the block — f is called with no arguments, and everything inside allocates from the frame by default:\n\nscratchspace() do\n    tmp = alloc(Float32, n)      # carved from this block's frame\n    y = softmax(x)               # an Allocating verb: outputs too\nend                              # frame retracts\n\nThe invariant both forms maintain: the ambient space is always the innermost open frame. The closure receives no frame because it needs no frame — an allocation that must outlive the block is the case that deserves the explicit space-first spelling.\n\n\n\n\n\n","title":"Pol.scratchspace"},{"category":"type","location":"scratchspaces/#Pol.Frame","page":"Scratchspaces","text":"Frame\n\nThe abstract supertype of allocation frames — the framed space a scratchspace block passes. A frame is a Space whose buffers are reclaimed when its block closes, and proof of being inside one: the one-step scratch form materializes against a Frame only.\n\n\n\n\n\n","title":"Pol.Frame"},{"category":"function","location":"scratchspaces/#Pol.release!","page":"Scratchspaces","text":"release!(buffer)\n\nRelease one frame buffer back to its space when a scratchspace block closes. The default is a no-op — GC-owned buffers just become garbage. Array families that benefit from eager reclamation overload it on the buffer type; a package extension already provides release!(x::AbstractGPUArray) = GPUArrays.unsafe_free!(x), freeing the device memory immediately instead of when the GC finalizes the array.\n\n\n\n\n\n","title":"Pol.release!"},{"category":"section","location":"reference/#API-reference","page":"API reference","text":"Every exported and public symbol, documented on the manual pages and indexed here.\n\n","title":"API reference"},{"category":"section","location":"descriptions/#Descriptions","page":"Descriptions","text":"A kernel describes the buffers it needs as data, holding no memory, and lets the caller decide where they come from. The atom is Undef — an element type and a shape — which alloc materializes against a Space of the caller's choosing.\n\nA kernel bundles its Undefs into named descriptions, dispatched on the kernel function and split by lifetime:\n\noutputs — the arrays a pass produces. They belong to the caller, so they are materialized from the unframed space and outlive the scratchspace block.\ncheckpoints — buffers a forward pass saves and its backward pass reads (a norm's Rstd, attention's M/L). They bridge forward → backward.\nscratch — buffers a single pass needs only while it runs. They live and die within a frame; the one-step form materializes against a Frame only.","title":"Descriptions"},{"category":"type","location":"descriptions/#Pol.Undef","page":"Descriptions","text":"Undef(T, dims)\nUndef(T, dims...)\nUndef(x, [T=eltype(x)])\nUndef(x, [dims=size(x)])\n\nA description of the element type and size of an array, holding no memory. Array{T}(undef, dims) allocates the array; Undef{T}(dims) only describes one, for alloc to allocate against a space of the caller's choosing.\n\n\n\n\n\n","title":"Pol.Undef"},{"category":"method","location":"descriptions/#Pol.alloc-Union{Tuple{T}, Tuple{Pol.Space, Undef{T}}} where T","page":"Descriptions","text":"alloc(space, u::Undef)\nalloc(space, spec::NamedTuple)\n\nMaterialize an Undef description — or a whole NamedTuple spec of them, name for name — from space. Specs nest: a NamedTuple value materializes recursively, so buffers bundled under one name stay bundled. A spec value that is already an array passes through untouched — merge an owned array over its Undef before the alloc and that buffer is never materialized, which is how a caller aliases one output in place (e.g. S′ = S) while the rest allocate.\n\n\n\n\n\n","title":"Pol.alloc"},{"category":"function","location":"descriptions/#Pol.outputs","page":"Descriptions","text":"outputs(f, args...; kwargs...) -> NamedTuple\noutputs(space, f, args...; kwargs...)\n\nThe arrays a pass produces — the destinations, first in the mutating signature — described as Undefs. Implementations dispatch on the pass function. A leading Space materializes the description on the spot: outputs(space, f, args...) is alloc(space, outputs(f, args...)). Outputs belong to the caller: materialize them from the unframed space, before the scratchspace block they must outlive.\n\n\n\n\n\n","title":"Pol.outputs"},{"category":"function","location":"descriptions/#Pol.checkpoints","page":"Descriptions","text":"checkpoints(f, args...; kwargs...) -> NamedTuple\ncheckpoints(space, f, args...; kwargs...)\n\nThe buffers a primitive saves in its forward pass and reads in its backward pass (a norm's Rstd, attention's M/L), described as Undefs: the primitive states eltypes and shapes, allocates nothing. Implementations dispatch on the forward function. A primitive that saves nothing has no method. The described names are keyword arguments the primitive already takes; a leading Space materializes them — splat the result into the call.\n\nDistinct from scratch, which lives and dies within a scope.\n\n\n\n\n\n","title":"Pol.checkpoints"},{"category":"function","location":"descriptions/#Pol.scratch","page":"Descriptions","text":"scratch(f, args...; kwargs...) -> NamedTuple\nscratch(frame::Frame, f, args...; kwargs...)\n\nThe buffers a single pass needs only while it runs (workspace, partial-reduction buffers), described as Undefs. Dispatched per pass function — rms_norm! and ∇rms_norm! each describe their own; a pass that needs none has no method. The described names are keyword arguments the pass already takes; materialize them against the Frame a scratchspace block passes — scratch(frame, f, args...) is alloc(frame, scratch(f, args...)) — and splat into the call. The one-step form takes a Frame only: scratch lifetime is frame lifetime.\n\nDistinct from checkpoints, which bridges forward → backward.\n\n\n\n\n\n","title":"Pol.scratch"},{"category":"section","location":"#Pol","page":"Home","text":"Memory management primitives: protocols for kernels to describe the buffers they need as data, one verb that materializes descriptions into a space of the caller's choosing, and a bump-allocator with frame-scoped lifetimes.","title":"Pol"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nRegistry.add(url=\"https://registry.jool.space\")\nPkg.add(\"Pol\")","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"using Pol\n\n# a kernel takes its buffers as plain keyword arguments and describes them:\nsoftmax!(y, x; tmp) = (tmp .= exp.(x); y .= tmp ./ sum(tmp, dims=1))\nPol.outputs(::typeof(softmax!), x) = (; y = Pol.Undef(x))\nPol.scratch(::typeof(softmax!), x) = (; tmp = Pol.Undef(x))\n\nx, y = rand(1000), zeros(1000)\n\n# materialize from an arena — the call is its own frame: carve, run, retract\narena = Arena(Vector{UInt8}(undef, 2^20))\n\nscratchspace(arena) do frame\n    softmax!(y, x; scratch(frame, softmax!, x)...)\nend\n\n# the functional form: output from the unframed space (caller lifetime),\n# scratch inside the frame (dies with the block)\nfunction softmax(x; space = Similar(x))\n    (; y) = outputs(space, softmax!, x)\n    scratchspace(space) do frame\n        softmax!(y, x; scratch(frame, softmax!, x)...)\n    end\n    return y\nend\n\nThat functional shell is what Allocating writes for you — with a NamedTuple-out verb softmax!((; y), x; tmp) it is one line, callable explicitly or against the ambient space:\n\nconst softmax = Allocating(softmax!)\n\ny = softmax(arena, x).y      # outputs from the arena, scratch framed\nwithspace(arena) do\n    y = softmax(x).y         # same call, space from scope\nend\n\nArena allocations are aligned arrays (256-byte by default; see Arena(slab; alignment)) aliasing a single preallocated byte slab — for a plain Vector{UInt8} slab they are unsafe_wrapped Arrays, not GC-rooted. They are only valid until the scratchspace block they were carved in closes. Arenas are task-local and never grow; allocating past the end throws.","title":"Quick start"},{"category":"section","location":"#Manual","page":"Home","text":"Spaces — the alloc verb, the Space it materializes against, the GC-owned Similar default, and the Arena bump allocator.\nScratchspaces — scratchspace blocks and frame-scoped lifetimes.\nDescriptions — how a kernel describes the buffers it needs as Undefs: its outputs, checkpoints, and scratch.\nVerbs — the Allocating form of a mutating verb.\nShadows — pairing primals with gradient buffers for reverse-mode kernels.\n\nThe full symbol index lives in the API reference.","title":"Manual"},{"category":"section","location":"shadows/#Shadows","page":"Shadows","text":"","title":"Shadows"},{"category":"type","location":"shadows/#Pol.Shadowed","page":"Shadows","text":"Shadowed(primal, shadow)\nShadowed(primal, [T=eltype(primal)])\nShadowed(space::Space, primal, [T=eltype(primal)])\n\nA primal array paired with a gradient buffer, passed positionally to in-place ∇-kernels: the backward pass reads primals and writes input gradients into shadows. Shadows are overwritten, never accumulated — cross-call accumulation is the AD backend's job. (Same shape as Enzyme's Duplicated, deliberately not its name: Enzyme's shadows accumulate.)\n\nThe shadow need not share the primal's eltype — a narrow-precision forward usually pairs with wider gradients (Shadowed(x, BFloat16) for an fp8 x); the backward kernel writes in the shadow's eltype. The one-argument form pairs x with an uninitialized similar(x) shadow, the type form with similar(x, T). A leading Space materializes the shadow from it instead: Shadowed(space, x, [T]). A shadow needs no description protocol — its spec is the primal itself.\n\nForward kernels accept a bare array or a Shadowed interchangeably via primal.\n\n\n\n\n\n","title":"Pol.Shadowed"},{"category":"function","location":"shadows/#Pol.primal","page":"Shadows","text":"primal(x)\n\nThe primal value of x: x itself for a bare array, or d.primal for a Shadowed. Lets forward kernels accept either.\n\n\n\n\n\n","title":"Pol.primal"},{"category":"function","location":"shadows/#Pol.shadow","page":"Shadows","text":"shadow(x)\n\nThe gradient shadow of a Shadowed, or nothing for anything else: a bare array passed where a Shadowed is accepted is a constant — no gradient is requested and none is written.\n\n\n\n\n\n","title":"Pol.shadow"},{"category":"section","location":"spaces/#Spaces","page":"Spaces","text":"Pol ships two spaces: the GC-owned Similar default and the Arena bump allocator. The Frames a scratchspace block opens are spaces too.","title":"Spaces"},{"category":"section","location":"spaces/#Similar","page":"Spaces","text":"","title":"Similar"},{"category":"section","location":"spaces/#Arena","page":"Spaces","text":"","title":"Arena"},{"category":"section","location":"spaces/#The-ambient-space","page":"Spaces","text":"The explicit space-first forms are the primitives; a dynamically-scoped binding removes the plumbing where it is noise. Every ambient method — alloc(T, dims...), an Allocating verb called without a leading space, a scratchspace block's defaults — forwards to its explicit form with ambientspace.","title":"The ambient space"},{"category":"section","location":"spaces/#Capture-safety","page":"Spaces","text":"","title":"Capture safety"},{"category":"type","location":"spaces/#Pol.Space","page":"Spaces","text":"Space\n\nThe abstract supertype of allocation spaces — anywhere an uninitialized array can be materialized from. A space implements the one leaf method alloc(space, T, dims::Dims); everything else — Undef specs, NamedTuple specs, space-first outputs/checkpoints and frame-first scratch — reduces to it.\n\n\n\n\n\n","title":"Pol.Space"},{"category":"method","location":"spaces/#Pol.alloc-Union{Tuple{T}, Tuple{Pol.Space, Type{T}, Vararg{Integer}}} where T","page":"Spaces","text":"alloc(space::Space, T, dims)\nalloc(space::Space, T, dims...)\n\nMaterialize the description of an array into space. A custom space implements the one leaf method alloc(space, T, dims::Dims).\n\n\n\n\n\n","title":"Pol.alloc"},{"category":"type","location":"spaces/#Pol.Similar","page":"Spaces","text":"Similar(x)\n\nThe GC-owned Space around an exemplar array: alloc against it is similar(x, T, dims) — same array family and device, lifetimes by scope. The exemplar is dispatched on, never read.\n\nEager-only: a carve goes through the device's allocator, so allocating from a Similar under an active graph capture throws CaptureViolation rather than silently recording a memory node into the graph. Under capture, use an Arena.\n\n\n\n\n\n","title":"Pol.Similar"},{"category":"type","location":"spaces/#Pol.Arena","page":"Spaces","text":"Arena(slab::AbstractVector{UInt8}; alignment = 256)\n\nA bump allocator over one preallocated byte slab: alloc carves typed arrays from it, retract! and reset! reclaim them in bulk, and nothing tracks individual allocations. The arena never grows — allocating past the end of the slab throws — and identical allocation sequences yield identical addresses, which is what graph capture requires.\n\nalignment (a power of two) is where each carve starts, relative to the slab's first byte; a carve additionally never starts below its element type's own alignment. Absolute alignment therefore also depends on the slab's base: CUDA allocators return 256-aligned bases (so the default meets cuBLASLt's workspace requirement), a Julia Vector's base is only 32/64, Mmap.mmap's is page-aligned.\n\nArenas are task-local, so they take no locks.\n\n\n\n\n\n","title":"Pol.Arena"},{"category":"method","location":"spaces/#Pol.alloc-Union{Tuple{T}, Tuple{Arena, Type{T}, NTuple{N, Int64} where N}} where T","page":"Spaces","text":"alloc(space::Arena, T, dims) -> AbstractArray{T}\n\nCarve a dims-shaped array of bitstype T from the arena: round the offset up to the arena's alignment (never below T's own), take the next sizeof(T) * prod(dims) bytes as a typed array (see carve), advance the offset, update the watermark. Throws past the end of the slab. The offset advance is source state, like an RNG's — hence no !.\n\n\n\n\n\n","title":"Pol.alloc"},{"category":"type","location":"spaces/#Pol.Mark","page":"Spaces","text":"Mark\n\nA snapshot of an Arena's offset, returned by mark and consumed by retract!. Carries the arena's epoch, so retracting to a mark taken before a reset! throws instead of silently corrupting newer allocations.\n\n\n\n\n\n","title":"Pol.Mark"},{"category":"function","location":"spaces/#Pol.mark","page":"Spaces","text":"mark(arena) -> Mark\n\nSnapshot the current offset. Does not change the arena.\n\n\n\n\n\n","title":"Pol.mark"},{"category":"function","location":"spaces/#Pol.retract!","page":"Spaces","text":"retract!(arena, m::Mark)\n\nMove the offset back to m: everything allocated since the mark was taken is garbage at once, however many carves that is. Retracting to the same mark twice with nothing allocated in between is a no-op. Throws if the mark is stale (taken before a reset!) or above the current offset.\n\n\n\n\n\n","title":"Pol.retract!"},{"category":"function","location":"spaces/#Pol.reset!","page":"Spaces","text":"reset!(arena)\n\nOffset back to 0 — every carve is garbage — and the epoch advances, so existing marks become stale. The watermark survives.\n\n\n\n\n\n","title":"Pol.reset!"},{"category":"function","location":"spaces/#Pol.watermark","page":"Spaces","text":"watermark(arena) -> Int\n\nThe peak offset ever reached — the measured byte requirement of everything run against this arena. Survives reset!, so it accumulates over all passes of a warmup.\n\n\n\n\n\n","title":"Pol.watermark"},{"category":"function","location":"spaces/#Pol.carve","page":"Spaces","text":"carve(slab, offset, T, dims) -> AbstractArray{T}\n\nA dims-shaped T array over slab[offset+1 : offset+sizeof(T)*prod(dims)], aliasing the bytes — never copying. The arena's only function that touches memory, and its per-storage specialization point. The generic method is reshape(reinterpret(T, view(slab, …)), dims); a Vector{UInt8} slab gets a native Array via unsafe_wrap instead — faster to index, not GC-rooted: valid only while the arena holding the slab is alive.\n\n\n\n\n\n","title":"Pol.carve"},{"category":"function","location":"spaces/#Pol.withspace","page":"Spaces","text":"withspace(f, space)\n\nCall f() with space as the ambient space for its dynamic extent. Inside, ambientspace returns space, and every ambient method — alloc(T, dims...), an Allocating verb called without a leading space — uses it. A scratchspace block rebinds the ambient space to its frame, so the ambient space is always the innermost open frame. The explicit space-first forms remain the primitives; the ambient forms forward to them, so this is one semantics with two spellings. An ambient read is dynamically dispatched (scope is runtime data) — thread spaces explicitly where full inference matters.\n\nScoped values flow into spawned tasks, but an Arena is task-local by design: don't Threads.@spawn under withspace of an arena.\n\n\n\n\n\n","title":"Pol.withspace"},{"category":"function","location":"spaces/#Pol.ambientspace","page":"Spaces","text":"ambientspace() -> Space\nambientspace(default)\n\nThe ambient space bound by the nearest enclosing withspace or scratchspace block. The zero-arg form throws when no space is bound; ambientspace(default) returns default instead — the form for kernel space keyword defaults, which must also work unscoped:\n\nspace = ambientspace(Similar(X))\n\n\n\n\n\n","title":"Pol.ambientspace"},{"category":"method","location":"spaces/#Pol.alloc-Tuple","page":"Spaces","text":"alloc(args...)\n\nThe ambient form: any alloc call without a leading space forwards to alloc(ambientspace(), args...) — every explicit spelling (T, dims, an Undef, a NamedTuple spec) has its ambient counterpart through this one method.\n\n\n\n\n\n","title":"Pol.alloc"},{"category":"function","location":"spaces/#Pol.capturing","page":"Spaces","text":"capturing(x) -> Bool\n\nWhether the device backing x is currently recording a graph capture.\n\nfalse for anything that cannot capture — every CPU array, and any backend without an extension answering otherwise. A backend extension adds a method on its array type (Pol.capturing(::CuArray)), so this is the one predicate the rest of the stack asks.\n\n\n\n\n\n","title":"Pol.capturing"},{"category":"type","location":"spaces/#Pol.CaptureViolation","page":"Spaces","text":"CaptureViolation\n\nA buffer was materialized from a garbage-collected Space while the device was recording a graph capture.\n\nThe allocation does not fail, and that is the problem. A device allocator is stream-ordered, so allocating under capture is recorded into the graph as a memory node — capture succeeds, instantiation succeeds, and the first launch succeeds. The second launch fails:\n\nERROR: CUDA error: invalid argument (code 1, ERROR_INVALID_VALUE)\n\nA graph holding an allocation node whose memory is not freed within the graph cannot be relaunched until that memory is freed — and replay is the whole reason the graph exists. So the error lands on exactly the operation the capture was built to perform, names nothing useful, and survives any smoke test that launches once. Freeing inside the captured region does not rescue it either: the free path errors during capture.\n\n(A lesser hazard rides along: if the pool is exhausted, the allocator escalates to reclaim, which device-synchronizes — and synchronizing under capture invalidates the capture outright. That fires only under memory pressure, so it is the same class of bug, deferred further.)\n\nThe capture-safe space is an Arena: a carve is offset arithmetic over a slab allocated long before capture began, so it records nothing into the graph, and an identical allocation sequence yields identical addresses — which is what replay requires. Warm up eagerly, read the watermark, size the slab, then capture.\n\nWarmth is not optional for the rest of the step either: a kernel's first launch loads its module, which capture forbids (ERROR_STREAM_CAPTURE_UNSUPPORTED). Run the step once eagerly before capturing it — the same pass that fills the watermark.\n\nReaching this error means a pass fell back to its eager default — a Similar space, or a scratchspace on one — inside a captured region. Pass an arena (or a Frame on one) instead.\n\n\n\n\n\n","title":"Pol.CaptureViolation"}]
}
