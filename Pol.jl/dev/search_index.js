var documenterSearchIndex = {"docs":
[{"category":"section","location":"spaces/#Spaces","page":"Spaces","text":"A Space is anywhere an array can be materialized from. The one verb alloc turns an Undef description — an element type and a shape, no memory — into a real array against the space the caller chose; every space reduces to the single leaf method alloc(space, T, dims::Dims).\n\nSimilar is the GC-owned space around an exemplar array: alloc against it is similar(x, T, dims) — same array family and device, lifetimes by scope — the natural default when no arena is in play. The bump-allocator Arena and the frames a scratchspace opens are spaces too; what a kernel materializes from them is covered under Descriptions.","title":"Spaces"},{"category":"type","location":"spaces/#Pol.Similar","page":"Spaces","text":"Similar(x)\n\nThe GC-owned space around an exemplar array: alloc against it is similar(x, T, dims) — buffers of x's array family, on x's device, with lifetimes by scope. The exemplar is dispatched on, never read.\n\n\n\n\n\n","title":"Pol.Similar"},{"category":"type","location":"spaces/#Pol.Space","page":"Spaces","text":"Space\n\nThe abstract supertype of allocation spaces — anywhere an uninitialized array can be materialized from. A space implements the one leaf method alloc(space, T, dims::Dims); everything else — Undef specs, NamedTuple specs, space-first outputs/checkpoints and frame-first scratch — reduces to it.\n\n\n\n\n\n","title":"Pol.Space"},{"category":"method","location":"spaces/#Pol.alloc-Union{Tuple{T}, Tuple{Any, Type{T}, Vararg{Integer}}} where T","page":"Spaces","text":"alloc(space, T, dims...) -> AbstractArray{T}\nalloc(space, u::Undef)\nalloc(space, spec::NamedTuple)\n\nMaterialize an uninitialized array — or a whole spec of Undef descriptions, name for name — from space. Specs nest: a NamedTuple value materializes recursively, so buffers bundled under one name stay bundled. The spaces:\n\nan Arena carves from its slab, reclaimed by retract!/reset!\na Similar exemplar gives similar(x, T, dims) — GC-owned, same array family and device\n\nA custom space implements the one leaf method alloc(space, T, dims::Dims).\n\n\n\n\n\n","title":"Pol.alloc"},{"category":"section","location":"scratchspaces/#Scratchspaces","page":"Scratchspaces","text":"A scratchspace block opens a Frame on a space and guarantees that everything allocated from the frame is reclaimed when the block closes — including on a throw. On an Arena the frame carries a mark and retracts to it; on any other space it records each buffer it hands out and release!s them at close.\n\nThis is the mechanism behind the lifetime split in the quick start: buffers a pass needs only while it runs are allocated inside the frame and die with the block, while buffers the caller must outlive are allocated from the unframed space before the block opens. Frames nest — opening one on a frame stacks a new level on the same space.\n\nrelease! is a no-op for GC-owned arrays but the extension point for array families with eager reclamation (e.g. CUDA.unsafe_free!).","title":"Scratchspaces"},{"category":"type","location":"scratchspaces/#Pol.Frame","page":"Scratchspaces","text":"Frame\n\nThe abstract supertype of allocation frames — the framed space a scratchspace block passes. A frame is a Space whose buffers are reclaimed when its block closes, and proof of being inside one: the one-step scratch form materializes against a Frame only.\n\n\n\n\n\n","title":"Pol.Frame"},{"category":"method","location":"scratchspaces/#Pol.release!-Tuple{Any}","page":"Scratchspaces","text":"release!(buffer)\n\nRelease one frame buffer back to its space when a scratchspace block closes. The default is a no-op — GC-owned buffers just become garbage. Array families that benefit from eager reclamation overload it on the buffer type, e.g. Pol.release!(x::CuArray) = CUDA.unsafe_free!(x) returns memory to the pool without waiting for the GC.\n\n\n\n\n\n","title":"Pol.release!"},{"category":"method","location":"scratchspaces/#Pol.scratchspace-Tuple{Function, Pol.Space}","page":"Scratchspaces","text":"scratchspace(f, space)\n\nAn allocation frame on space: calls f with a Frame —\n\nscratchspace(space) do frame\n    softmax!(y, x; scratch(frame, softmax!, x)...)\nend\n\nOn an Arena the frame carries a mark, retracted when the block closes, also on a throw — everything carved inside is reclaimed. On any other space the frame records each buffer it materializes and release!s them all at close — a no-op for GC-owned arrays, eager reclamation where an overload provides it. Frames nest: opening a frame on a frame stacks a new one on the same space.\n\nBuffers allocated from the frame are only valid inside the block. Allocating from the unframed space — outputs before the block opens — is what gives a buffer the caller's lifetime.\n\n\n\n\n\n","title":"Pol.scratchspace"},{"category":"section","location":"reference/#API-reference","page":"API reference","text":"Every exported and public symbol, documented on the manual pages and indexed here.\n\n","title":"API reference"},{"category":"section","location":"arenas/#Arenas","page":"Arenas","text":"An Arena is a bump allocator over one preallocated byte slab: alloc carves typed arrays from it by advancing an offset, and reclamation happens in bulk rather than per allocation. Because identical allocation sequences yield identical addresses, an arena is what graph capture (e.g. CUDA graphs) requires.\n\nReclamation comes in two forms. mark snapshots the current offset and retract! rolls back to it — everything carved since the mark becomes garbage at once. reset! rolls all the way back to zero and advances the epoch, invalidating outstanding marks. The watermark records the peak offset ever reached and survives reset!, so a warmup pass measures the exact byte budget a workload needs.\n\nArenas never grow — allocating past the end of the slab throws — and are task-local, so they take no locks.","title":"Arenas"},{"category":"type","location":"arenas/#Pol.Arena","page":"Arenas","text":"Arena(slab::AbstractVector{UInt8}; alignment = 256)\n\nA bump allocator over one preallocated byte slab: alloc carves typed arrays from it, retract! and reset! reclaim them in bulk, and nothing tracks individual allocations. The arena never grows — allocating past the end of the slab throws — and identical allocation sequences yield identical addresses, which is what graph capture requires.\n\nalignment (a power of two) is where each carve starts, relative to the slab's first byte; a carve additionally never starts below its element type's own alignment. Absolute alignment therefore also depends on the slab's base: CUDA allocators return 256-aligned bases (so the default meets cuBLASLt's workspace requirement), a Julia Vector's base is only 32/64, Mmap.mmap's is page-aligned.\n\nArenas are task-local: no lock, no sharing across tasks.\n\n\n\n\n\n","title":"Pol.Arena"},{"category":"type","location":"arenas/#Pol.Mark","page":"Arenas","text":"Mark\n\nA snapshot of an Arena's offset, returned by mark and consumed by retract!. Carries the arena's epoch, so retracting to a mark taken before a reset! throws instead of silently corrupting newer allocations.\n\n\n\n\n\n","title":"Pol.Mark"},{"category":"method","location":"arenas/#Pol.alloc-Union{Tuple{T}, Tuple{Arena, Type{T}, NTuple{N, Int64} where N}} where T","page":"Arenas","text":"alloc(arena::Arena, T, dims...) -> AbstractArray{T}\n\nCarve a dims-shaped array of bitstype T from the arena: round the offset up to the arena's alignment (never below T's own), take the next sizeof(T) * prod(dims) bytes as a typed array (see carve), advance the offset, update the watermark. Throws past the end of the slab. The offset advance is source state, like an RNG's — hence no !.\n\n\n\n\n\n","title":"Pol.alloc"},{"category":"method","location":"arenas/#Pol.carve-Union{Tuple{T}, Tuple{AbstractVector{UInt8}, Int64, Type{T}, NTuple{N, Int64} where N}} where T","page":"Arenas","text":"carve(slab, offset, T, dims) -> AbstractArray{T}\n\nA dims-shaped T array over slab[offset+1 : offset+sizeof(T)*prod(dims)], aliasing the bytes — never copying. The arena's only function that touches memory, and its per-storage specialization point. The generic method is reshape(reinterpret(T, view(slab, …)), dims); a Vector{UInt8} slab gets a native Array via unsafe_wrap instead — faster to index, not GC-rooted: valid only while the arena holding the slab is alive.\n\n\n\n\n\n","title":"Pol.carve"},{"category":"method","location":"arenas/#Pol.mark-Tuple{Arena}","page":"Arenas","text":"mark(arena) -> Mark\n\nSnapshot the current offset. Does not change the arena.\n\n\n\n\n\n","title":"Pol.mark"},{"category":"method","location":"arenas/#Pol.reset!-Tuple{Arena}","page":"Arenas","text":"reset!(arena)\n\nOffset back to 0 — every carve is garbage — and the epoch advances, so existing marks become stale. The watermark survives.\n\n\n\n\n\n","title":"Pol.reset!"},{"category":"method","location":"arenas/#Pol.retract!-Tuple{Arena, Pol.Mark}","page":"Arenas","text":"retract!(arena, m::Mark)\n\nMove the offset back to m: everything allocated since the mark was taken is garbage at once, however many carves that is. Retracting to the same mark twice with nothing allocated in between is a no-op. Throws if the mark is stale (taken before a reset!) or above the current offset.\n\n\n\n\n\n","title":"Pol.retract!"},{"category":"method","location":"arenas/#Pol.watermark-Tuple{Arena}","page":"Arenas","text":"watermark(arena) -> Int\n\nThe peak offset ever reached — the measured byte requirement of everything run against this arena. Survives reset!, so it accumulates over all passes of a warmup.\n\n\n\n\n\n","title":"Pol.watermark"},{"category":"section","location":"descriptions/#Descriptions","page":"Descriptions","text":"A kernel describes the buffers it needs as data, holding no memory, and lets the caller decide where they come from. The atom is Undef — an element type and a shape — which alloc materializes against a Space of the caller's choosing.\n\nA kernel bundles its Undefs into named descriptions, dispatched on the kernel function and split by lifetime:\n\noutputs — the arrays a pass produces. They belong to the caller, so they are materialized from the unframed space and outlive the scratchspace block.\ncheckpoints — buffers a forward pass saves and its backward pass reads (a norm's Rstd, attention's M/L). They bridge forward → backward.\nscratch — buffers a single pass needs only while it runs. They live and die within a frame; the one-step form materializes against a Frame only.\n\nEach protocol has a space-first (or frame-first) form that materializes the description on the spot, ready to splat into the call. A pass that needs none of a given kind simply defines no method.","title":"Descriptions"},{"category":"type","location":"descriptions/#Pol.Undef","page":"Descriptions","text":"Undef(T, dims)\nUndef(T, dims...)\nUndef(x, [T=eltype(x)])\nUndef(x, [dims=size(x)])\n\nA description of the element type and size of an array, holding no memory. Array{T}(undef, dims) allocates the array; Undef{T}(dims) only describes one, for alloc to allocate against a space of the caller's choosing.\n\n\n\n\n\n","title":"Pol.Undef"},{"category":"function","location":"descriptions/#Pol.checkpoints","page":"Descriptions","text":"checkpoints(f, args...; kwargs...) -> NamedTuple\ncheckpoints(space, f, args...; kwargs...)\n\nThe buffers a primitive saves in its forward pass and reads in its backward pass (a norm's Rstd, attention's M/L), described as Undefs: the primitive states eltypes and shapes, allocates nothing. Implementations dispatch on the forward function. A primitive that saves nothing has no method. The described names are keyword arguments the primitive already takes; a leading Space materializes them — splat the result into the call.\n\nDistinct from scratch, which lives and dies within a scope.\n\n\n\n\n\n","title":"Pol.checkpoints"},{"category":"function","location":"descriptions/#Pol.outputs","page":"Descriptions","text":"outputs(f, args...; kwargs...) -> NamedTuple\noutputs(space, f, args...; kwargs...)\n\nThe arrays a pass produces — the destinations, first in the mutating signature — described as Undefs. Implementations dispatch on the pass function. A leading Space materializes the description on the spot: outputs(space, f, args...) is alloc(space, outputs(f, args...)). Outputs belong to the caller: materialize them from the unframed space, before the scratchspace block they must outlive.\n\n\n\n\n\n","title":"Pol.outputs"},{"category":"function","location":"descriptions/#Pol.scratch","page":"Descriptions","text":"scratch(f, args...; kwargs...) -> NamedTuple\nscratch(frame::Frame, f, args...; kwargs...)\n\nThe buffers a single pass needs only while it runs (workspace, partial-reduction buffers), described as Undefs. Dispatched per pass function — rms_norm! and ∇rms_norm! each describe their own; a pass that needs none has no method. The described names are keyword arguments the pass already takes; materialize them against the Frame a scratchspace block passes — scratch(frame, f, args...) is alloc(frame, scratch(f, args...)) — and splat into the call. The one-step form takes a Frame only: scratch lifetime is frame lifetime.\n\nDistinct from checkpoints, which bridges forward → backward.\n\n\n\n\n\n","title":"Pol.scratch"},{"category":"section","location":"#Pol","page":"Home","text":"Memory management primitives: protocols for kernels to describe the buffers they need as data, one verb that materializes descriptions into a space of the caller's choosing, and a bump-allocator with frame-scoped lifetimes.","title":"Pol"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nRegistry.add(\"https://registry.jool.space\")\nPkg.add(url = \"https://github.com/jool-space/Pol.jl\")","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"using Pol\n\n# a kernel takes its buffers as plain keyword arguments and describes them:\nsoftmax!(y, x; tmp) = (tmp .= exp.(x); y .= tmp ./ sum(tmp, dims=1))\nPol.outputs(::typeof(softmax!), x) = (; y = Pol.Undef(x))\nPol.scratch(::typeof(softmax!), x) = (; tmp = Pol.Undef(x))\n\nx, y = rand(1000), zeros(1000)\n\n# materialize from an arena — the call is its own frame: carve, run, retract\narena = Arena(Vector{UInt8}(undef, 2^20))\n\nscratchspace(arena) do frame\n    softmax!(y, x; scratch(frame, softmax!, x)...)\nend\n\n# the functional form: output from the unframed space (caller lifetime),\n# scratch inside the frame (dies with the block)\nfunction softmax(x; space = Similar(x))\n    (; y) = outputs(space, softmax!, x)\n    scratchspace(space) do frame\n        softmax!(y, x; scratch(frame, softmax!, x)...)\n    end\n    return y\nend\n\nArena allocations are aligned arrays (256-byte by default; see Arena(slab; alignment)) aliasing a single preallocated byte slab — for a plain Vector{UInt8} slab they are unsafe_wrapped Arrays, not GC-rooted. They are only valid until the scratchspace block they were carved in closes. Arenas are task-local and never grow; allocating past the end throws.","title":"Quick start"},{"category":"section","location":"#Manual","page":"Home","text":"Spaces — the alloc verb, the Space it materializes against, and the GC-owned Similar default.\nArenas — the Arena bump allocator, marks, and bulk reclamation.\nScratchspaces — scratchspace blocks and frame-scoped lifetimes.\nDescriptions — how a kernel describes the buffers it needs as Undefs: its outputs, checkpoints, and scratch.\nShadows — pairing primals with gradient buffers for reverse-mode kernels.\n\nThe full symbol index lives in the API reference.","title":"Manual"},{"category":"section","location":"shadows/#Shadows","page":"Shadows","text":"A Shadowed pairs a primal array with a gradient buffer for in-place reverse-mode kernels: the backward pass reads primals and writes input gradients into shadows. Shadows are overwritten, never accumulated — cross-call accumulation is the AD backend's job. (This is the shape of Enzyme's Duplicated, deliberately not its name, whose shadows accumulate.)\n\nThe shadow need not share the primal's eltype — a narrow-precision forward usually pairs with wider gradients — and a leading Space materializes the shadow from it. Forward kernels stay agnostic: primal unwraps a Shadowed or passes a bare array through, and shadow returns the gradient buffer or nothing, so a bare array passed where a Shadowed is accepted reads as a constant with no gradient requested.","title":"Shadows"},{"category":"type","location":"shadows/#Pol.Shadowed","page":"Shadows","text":"Shadowed(primal, shadow)\nShadowed(primal)\nShadowed(primal, T::Type)\n\nA primal array paired with a gradient buffer, passed positionally to in-place ∇-kernels: the backward pass reads primals and writes input gradients into shadows. Shadows are overwritten, never accumulated — cross-call accumulation is the AD backend's job. (Same shape as Enzyme's Duplicated, deliberately not its name: Enzyme's shadows accumulate.)\n\nThe shadow need not share the primal's eltype — a narrow-precision forward usually pairs with wider gradients (Shadowed(x, BFloat16) for an fp8 x); the backward kernel writes in the shadow's eltype. The one-argument form pairs x with an uninitialized similar(x) shadow, the type form with similar(x, T). A leading Space materializes the shadow from it instead: Shadowed(space, x, [T]). A shadow needs no description protocol — its spec is the primal itself.\n\nForward kernels accept a bare array or a Shadowed interchangeably via primal.\n\n\n\n\n\n","title":"Pol.Shadowed"},{"category":"method","location":"shadows/#Pol.primal-Tuple{Any}","page":"Shadows","text":"primal(x)\n\nThe primal value of x: x itself for a bare array, or d.primal for a Shadowed. Lets forward kernels accept either.\n\n\n\n\n\n","title":"Pol.primal"},{"category":"method","location":"shadows/#Pol.shadow-Tuple{Any}","page":"Shadows","text":"shadow(x)\n\nThe gradient shadow of a Shadowed, or nothing for anything else: a bare array passed where a Shadowed is accepted is a constant — no gradient is requested and none is written.\n\n\n\n\n\n","title":"Pol.shadow"}]
}
